GitHub:
Upload the whole folder as one repository.

Railway backend:
Root Directory: backend
Start Command: npx prisma db push && node server.js

Railway full-site mode:
Set VITE_API_URL=https://www.cafeslux.com
