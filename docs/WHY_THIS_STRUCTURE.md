## Why this is the final structure
- The React frontend is the canonical live site.
- The backend API is isolated for Railway.
- Old static HTML files are archived and no longer conflict with the live frontend.
- Public menu images belong in `frontend/public/menu/`.
